# tests/integration/uitest

PTY-based UI tests using `creack/pty` + `hinshun/vt10x` per CLAUDE.md § 3.9
Layer 2. Cell-grid metadata compared against golden files.

Reserved for spec-0.11.5-ui-review-pipeline.

